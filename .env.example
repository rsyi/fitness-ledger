# Copy to .env and fill in values. .env is gitignored.
# Used at build time by `dart run ~/repos/airledger/tool/brand.dart`.

# Path to your Google Cloud service-account JSON key file.
# The file's contents will be read and inlined into assets/config.yaml.
GOOGLE_SA_KEY_PATH=~/.config/airledger/service-account.json
